<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>{{ title }}</h1>
    <ul>
    {{#top}}
        <li><a href="{{homepage}}" target="_blank">{{title}}</a> ({{totalDownloads}})</li>
    {{/top}}
    </ul>
  </body>
</html>
